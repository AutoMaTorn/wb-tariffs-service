# WB Tariffs Service

Сервис для регулярного получения информации о тарифах Wildberries и сохранения в БД с синхронизацией в Google Sheets.

## 📋 Требования

- Docker
- Docker Compose

## 🚀 Быстрый запуск

1. Клонируйте репозиторий:
```bash
git clone https://github.com/AutoMaTorn/wb-tariffs-service
cd wb-tariffs-service
```

2. Создайте файл `.env`:
```bash
cp .env.example .env
```

3. Запустите приложение:
```bash
docker-compose up
```

Приложение запустится и начнет работу автоматически.

## ⚙️ Настройка окружения

Перед первым запуском отредактируйте файл `.env`:

```env
# Токен WB API (будет предоставлен через hh.ru)
WB_API_TOKEN=your_wb_api_token_here

# Данные для Google Sheets API
GOOGLE_SERVICE_ACCOUNT_EMAIL=your-service-account@project.iam.gserviceaccount.com
GOOGLE_PRIVATE_KEY=-----BEGIN PRIVATE KEY-----\nYOUR_PRIVATE_KEY\n-----END PRIVATE KEY-----
SPREADSHEET_IDS=spreadsheet_id_1,spreadsheet_id_2

# Настройки базы данных (оставить как есть)
DB_HOST=db
DB_PORT=5432
DB_NAME=postgres
DB_USER=postgres
DB_PASSWORD=postgres
```

## 🔍 Проверка работы приложения

### 1. Проверка логов
```bash
docker-compose logs -f app
```

Успешный запуск покажет:
```
🚀 Starting WB Tariffs Service...
📦 Initializing services...
⏰ Tariff scheduler started (runs hourly)
✅ WB Tariffs Service started successfully
📅 Processing tariffs for date: 2024-01-15
💾 Saved/updated X tariff records
```

### 2. Проверка базы данных
```bash
# Подключение к БД
docker-compose exec db psql -U postgres -d postgres

# Проверка данных
SELECT COUNT(*) FROM box_tariffs;
SELECT date, warehouse_name, box_storage_coef_expr 
FROM box_tariffs 
ORDER BY created_at DESC 
LIMIT 5;
```

### 3. Проверка Google Sheets
- Откройте ваши Google таблицы
- Проверьте лист `stocks_coefs`
- Данные должны обновляться ежечасно

## 🛠 Технические детали

### Архитектура
- **Node.js** + **TypeScript**
- **PostgreSQL** с **Knex.js** 
- **Google Sheets API**
- **Docker** контейнеризация

### Основные функции
- Ежечасное получение тарифов с WB API
- Сохранение в PostgreSQL
- Автоматическое обновление Google Sheets
- Обработка ошибок и повторные попытки

### Расписание
- Обновление тарифов: каждый час
- Синхронизация с Google Sheets: после каждого обновления

## 🔧 Управление приложением

### Команды Docker
```bash
# Запуск
docker-compose up

# Запуск в фоне
docker-compose up -d

# Остановка
docker-compose down

# Просмотр логов
docker-compose logs -f app

# Пересборка
docker-compose up --build
```

### Структура БД
Таблица `box_tariffs` содержит информацию о тарифах:
- Дата и склад
- Параметры доставки и хранения
- Географический регион
- Временные метки

### Логи для отладки
```bash
# Подробные логи приложения
docker-compose logs app

# Логи базы данных
docker-compose logs db

# Проверка статуса контейнеров
docker-compose ps
```

## 📁 Файлы конфигурации

- `.env.example` - шаблон для настроек окружения
- `docker-compose.yml` - конфигурация Docker
- `knexfile.ts` - настройки базы данных
- `tsconfig.json` - конфигурация TypeScript

## 🔄 Процесс работы

1. **Запуск** → Миграции БД → Инициализация сервисов
2. **Ежечасно** → Запрос тарифов WB → Сохранение в БД
3. **Синхронизация** → Обновление Google Sheets
4. **Логирование** → Все этапы записываются в логи

---

**Приложение готово к работе!** Для запуска достаточно выполнить `docker-compose up` после настройки `.env` файла.
